spring:  
  application:
    name: gateway-service
  cloud:
    consul:
      host: localhost
      port: 8500
    
      discovery:
        register: false
        registerHealthCheck: false

server:  
  port: 8080
